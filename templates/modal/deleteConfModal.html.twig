<div class="modal fade" id="deleteReservationConfModal" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Suppprimer la réservation : {{ reservation.id }}</h5>
                <button type="button" class="close btn btn-close" data-dismiss="modal" aria-label="Close">
                </button>
            </div>
            <div class="modal-body">
                <p>Êtes-vous sûr de vouloir supprimer cette réservation?</p>
            </div>
            <div class="modal-footer">
                {{ form_start(formDeleteconf, {'action' :path('app_delete_reservation',{'reservation': reservation.id })}) }}
                {{ form_end(formDeleteconf) }}
            </div>
        </div>
    </div>
</div>