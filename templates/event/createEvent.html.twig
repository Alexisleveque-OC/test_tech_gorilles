{% extends 'base.html.twig' %}

{% block title %}Création d'évènement{% endblock %}

{% block body %}
    <div class="offset-2 col-8">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary offser-2 col-4">
            {% if editMode %}
                Modification de l'évènement
            {% else %}
                Création de l'évènement
            {% endif %}
        </button>
        {{ form_end(form) }}
    </div>
{% endblock %}
